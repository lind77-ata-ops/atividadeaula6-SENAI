import pygame
pygame.init()

tela = pygame.display.set_mode([500, 500])

running = True
while running:
    pass
      
    tela.fill((255, 255, 255))
    pygame.draw.circle(tela, (0, 0, 255), (250, 250), 75)
    pygame.display.flip()
pygame.quit()